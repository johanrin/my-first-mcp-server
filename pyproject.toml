[project]
name = "my-first-mcp-server"
version = "0.1.0"
description = "An MCP server that provides weather information using the Open-Meteo API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.26.0",
]
